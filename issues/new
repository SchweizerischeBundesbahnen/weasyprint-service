## Summary

`pypdf==6.9.1` is currently listed in both `[project.dependencies]` and `[dependency-groups] test` in `pyproject.toml`. Since it is already a main project dependency, it is redundant in the `test` group and should be removed from there.

## Location

**File:** `pyproject.toml`, line 46

```toml
[dependency-groups]
test = [
    ...
    "pypdf==6.9.1",   # ← remove this line
    ...
]
```

`pypdf==6.9.1` is already declared in `[project.dependencies]` (line 23), so it will be available to tests transitively.

## Steps

- [ ] Remove `"pypdf==6.9.1",` from the `test` dependency group in `pyproject.toml`
- [ ] Verify tests still pass after the change